Features of Membership:
\begin{itemize}
    \item MEMBERSHIP\_STATUS (target) (string): \textbf{categorical (nominal)}
    \item MEMBERSHIP\_NUMBER (string):  \textbf{categorical (nominal)} (i'm not sure)
    \item MEMBERSHIP\_TERM\_YEARS (numeric): \textbf{numeric (ratio)}
    \item ANNUAL\_FEES (numeric): \textbf{numeric (ratio)}
    \item MEMBER\_MARITAL\_STATUS (string): \textbf{categorical (nominal)}
    \item MEMBER\_GENDER (string): \textbf{categorical (nominal)}
    \item MEMBER\_ANNUAL\_INCOME (numeric): \textbf{numeric (ratio)}
    \item MEMBER\_OCCUPATION\_CD (numeric): \textbf{categorical (nominal)}
    \item MEMBERSHIP\_PACKAGE (string): \textbf{categorical (nominal)}
    \item MEMBER\_AGE\_AT\_ISSUE (numeric):  \textbf{numeric (ratio)}
    \item ADDITIONAL\_MEMBERS (numeric): \textbf{numeric (ratio)}
    \item PAYMENT\_MODE (string): \textbf{categorical (ordinal)} (I'm not sure)
    \item AGENT\_CODE (string): \textbf{categorical (nominal)} (i'm not sure)
    \item START\_DATE (YYYYMMDD) (numeric): \textbf{numeric (interval)}
    \item END\_DATE (YYYYMMDD) (numeric): \textbf{numeric (interval)}
\end{itemize}

Features of Accident:
\begin{itemize}
    \item Accident (target) (nominal): \textbf{categorical (nominal)}
    \item Shift (nominal): \textbf{categorical (nominal)}
    \item Equipment (nominal):\textbf{ categorical (nominal)}
    \item Weather (nominal): \textbf{categorical (nominal)}
    \item Operator\_Experience (nominal): \textbf{categorical (ordinal)}
    \item Production\_Tons (numeric): \textbf{numeric (ratio)}
    \item Maintenance\_Hours (numeric): \textbf{numeric (ratio)}
\end{itemize}